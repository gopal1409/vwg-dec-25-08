#we need to be a root user to install jenkins
sudo -i
##same password Welcome@123
#jenkins run using java 
apt-get update
apt install default-jdk
apt install maven
##lets install jenkins also 
67  sudo wget -O /etc/apt/keyrings/jenkins-keyring.asc   https://pkg.jenkins.io/debian-stable/jenkins.io-2023.key
   68  echo "deb [signed-by=/etc/apt/keyrings/jenkins-keyring.asc]"   https://pkg.jenkins.io/debian-stable binary/ | sudo tee   /etc/apt/sources.list.d/jenkins.list > /dev/null
   69  sudo apt update
   70  sudo apt install jenkins
##once you install jenkins to check the jenkins installation
 systemctl status jenkins
#####################################p[ress q to comeout
#3once the jenkins installation complete
 cat /var/lib/jenkins/secrets/initialAdminPassword
##this will give the intial admin password 
#####################once you get the password 
#copy the dns name of the first vm1
lab-as-23886.lc.cl-labs.springpeople.com:808
